{# templates/components/patient_card.html.twig #}
<div class="bg-white rounded-lg shadow-sm p-4 hover:shadow-md transition-shadow">
    <div class="flex items-start justify-between">
        <div>
            <h3 class="text-lg font-semibold text-gray-800">{{ patient.nom }} {{ patient.prenom }}</h3>
            <p class="text-gray-600 text-sm mt-1">
                <i class="fas fa-envelope mr-2"></i>
                {{ patient.email }}
            </p>
        </div>
        <div class="flex space-x-2">
            <button class="text-blue-500 hover:text-blue-600" title="Éditer">
                <i class="fas fa-edit"></i>
            </button>
            <button class="text-red-500 hover:text-red-600" title="Supprimer">
                <i class="fas fa-trash"></i>
            </button>
        </div>
    </div>
</div>
